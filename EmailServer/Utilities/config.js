let sender_email = process.env.sender_email; 
let sender_password = process.env.sender_password; //real pass word is  Sit@123456789 and the provided one is apps password


module.exports = {
	sender_email: sender_email,
	sender_password: sender_password
};